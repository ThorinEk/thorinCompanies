package dev.gustavpersson.thorincompanies.business_logic_layer.constants

// These are the property key paths used in the config.yml file
object ConfigKeys {
    const val MAX_COMPANIES_PER_PLAYER = "max_companies_per_player"
    const val COMPANY_STARTUP_COST = "company_startup_cost"
    const val DATABASE_HOST = "database.hostname"
    const val DATABASE_PORT = "database.port"
    const val DATABASE_USER = "database.user"
    const val DATABASE_NAME = "database.dbname"
    const val DATABASE_PASSWORD = "database.password"
}
